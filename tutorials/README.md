# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/vit-classification-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/vit-classification-pipeline/blob/main/tutorials/vit_classification_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-timm%2Fvit__base__patch16__224.orig__in21k__ft__in1k-ffcc4d?style=flat)](https://huggingface.co/timm/vit_base_patch16_224.orig_in21k_ft_in1k)
[![Upstream](https://img.shields.io/badge/Upstream-huggingface%2Fpytorch--image--models-181717?style=flat&logo=github&logoColor=white)](https://github.com/huggingface/pytorch-image-models)
[![arXiv](https://img.shields.io/badge/arXiv-2010.11929-b31b1b.svg)](https://arxiv.org/abs/2010.11929)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's
pipeline modules (`pipeline.py`, `metrics.py`, `samples.py`), model identity, snapshot manifest and runtime pins, so the exported `.ipynb` keeps working
without this repository being reachable. Do not edit the notebook by hand; edit the package or the template
and regenerate (`python tools/build_notebook.py`; `--check` is enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `vit_classification_colab.ipynb` | `E2E` | `GUIDED` | standalone (generated) | ViT-B/16 ImageNet-1k classification and bounded supervised adaptation to a label space the checkpoint does not have — the safetensors snapshot digest-verified against the inline manifest before it is loaded; the ImageNet-1k inference contract (`validate_inputs` → input manifest with an oversized-image rejection probe; `predict` → argmax plus rank-ordered top-5 softmax scores) on three test photographs, three of the six species having no ImageNet class; a digest-pinned real image corpus (180 CC0 iNaturalist bird photographs, six species) validated and split without leakage, with observer overlap counted; the frozen 768-d pre-logits scored by a **majority floor**, a **cosine 5-NN vote** and a new six-way **linear probe** (frozen policy); a bounded unfreeze of the last two transformer blocks selected against the probe by validation log-loss (unfrozen policy); held-out **accuracy / macro-F1** with per-class recall and a four-way comparison; predictions before and after; safetensors adapter export (new head plus any trained blocks; never the ImageNet head) with verified reload parity including `predict` | CPU float32 (CUDA used automatically when present) | 180 CC0 iNaturalist photographs (19 MB) fetched at run time, one SHA-256 each in the carried record table | a `.zip` (or directory) with `labels.csv` beside the image files (gated off by default) | ~two min of model time on CPU after the 346 MB checkpoint and 19 MB of photographs | **Candidate** — see `../docs/release-verification.md` |

## What changed from the inference-only tutorial

The previous notebook (`TASK-INFERENCE`, classification obligations) classified a synthetic gradient and reported a `not-measurable` verdict because it had no ground truth. This notebook keeps the ImageNet-1k inference contract as Section 5 (on real test photographs, where the label-space gap is visible) and adds the adaptation contract around it: a real labelled photo corpus pinned per file, accuracy and macro-F1 in the carried `metrics.py` with a majority floor and a k-NN baseline as reference points, a new linear head (frozen policy) and a bounded unfreeze (unfrozen policy) selected on validation, before/after predictions, and an artifact that reloads. The per-batch `evaluation_report` still returns `not-measurable` (species labels are not ImageNet-1k indices); `pipe.evaluate` is the labelled corpus-level stage (`measured` / `measured-small-sample`).

## Conformance notes

- **Standalone carrier (ST1–ST8):** the primary path performs no clone, repository install, or repository import. Section 2 embeds the repository's 3 modules verbatim in topological dependency order (each tagged `metadata.dimer.embedded_module`; the only rewrites make `DEFAULT_WEIGHTS_DIR` working-directory-relative and remove internal relative package imports) — the 180-row pinned photo table travels inside the carried `samples.py` — Section 3 carries the 3-file snapshot manifest inline and asserts it against the module's constants before fetching, and `NOTEBOOK_SOURCE` records the repository, revision and module digests in the exports.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried modules, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Model acquisition (MOD1–MOD9):** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries a fresh clone lacks (the git-ignored `model.safetensors`), at the pinned revision; `verify_snapshot` re-hashes every entry; `ViTClassificationPipeline.from_pretrained(weights_dir=WEIGHTS_DIR)` loads the verified files through `timm` with the snapshot's own `pretrained_cfg`; no cell outside the carried modules calls `huggingface_hub`, `timm`, `safetensors`, `urllib`, `forward_features` or the model directly.
- **Data (DAT1–DAT9, ST6):** the default sample is 180 pinned objects — `<photo id>/medium.jpg` or `medium.jpeg`, each photo's own served extension recorded in the table from `inaturalist-open-data.s3.amazonaws.com`, 19,183,071 bytes in total, one SHA-256 each — every photograph CC0 by its own iNaturalist licence code, one per observer per species, each refused on any mismatch before it is decoded, and drawn by a seeded stratified shuffle into 108 / 24 / 48 records (`check_split_disjoint` asserts no shared photograph by decoded-pixel digest; `observer_overlap` reports the observers in more than one split). The template adds a second *External access* bullet naming the host, the byte count and the CC0 licence.
- **Stages (§4.1):** `fetch_corpus` + `read_corpus` + `build_sample_dataset` / `load_byod_dataset` + `split_dataset` (sample acquisition) → `validate_dataset` + `class_names` + `check_split_disjoint` + `observer_overlap` + refusal probes (validation, with three digests) → `validate_inputs` + `pipe.predict` with sanity checks and the three ImageNet top-5 lists (inference contract) → `majority_baseline` + `pipe.knn_baseline` + `pipe.adapt(trainable_blocks=0)` + `pipe.evaluate` (floor, baseline and frozen policy) → `pipe.adapt(trainable_blocks=TRAINABLE_BLOCKS)` (unfrozen policy, selected on validation) → `pipe.evaluate` on validation and test with the comparison (evaluation) → `pipe.classify` before and after + `evaluation_report` (inference) → `pipe.save_artifact` → `ViTClassificationPipeline.from_artifact` with the parity assertion including `predict` (export and reload).
- **Classification obligations (§20.1) and score semantics (UNC1–UNC4):** the ImageNet class count (1000) and the new head's class list are printed before the model runs, both decision rules are `argmax`, softmax scores are described as uncalibrated (no threshold is shipped), exported top-k scores keep their rank ordering; accuracy and macro-F1 are computed over one labelled set with a fixed class order.
- **Baselines and evidence (EVAL6/EVAL10/EVAL11):** the majority floor, the k-NN vote and the frozen policy are scored on the same 48 test photographs as the selected policy, per-class recall is printed beside every accuracy, the selection rule is stated (lowest validation log-loss, probe competing as epoch 0) and the notebook asserts only that the selected model beats the floor — a gain over the probe is the question, not an assertion; every metric is labelled as one seeded split of one small corpus with no dispersion estimate (one image ≈ 2.1 points).
- **Split integrity (SPL1–SPL8):** the split is seeded, stratified by species and image-disjoint by decoded-pixel digest; validation selects the epoch and policy and the test split is used for nothing but the final evaluation and the reload-parity check; the notebook counts observers in more than one split and states that real data must be split by photographer, session or device.
- **Ceilings (VAL6):** `NUM_CLASSES` (1000), `MAX_IMAGE_SIDE` (4,096), `MAX_BATCH` (64), `INPUT_SIZE` (224), `FEATURE_DIM` (768), the record bounds (8..20,000), the class bounds (2..100), the label pattern and the id pattern are stated in the Prerequisites and printed before the model runs; four dataset refusals and one image refusal are demonstrated.
- **BYOD (DAT10–DAT19):** `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog; when enabled, user images flow through the same validation, seeded stratified image-disjoint split, floors, both policies, evaluation, prediction, export and reload cells; images inside the uploaded zip are decoded from the archive, never extracted. The expected schema and the privacy guidance — photographs of people or private places are restricted data — are stated in the Prerequisites and in Section 4, and the training labels table is written to `outputs/vit_classification_train.csv` in that shape.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `../docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`.

## AI Assistance Disclosure

This repository’s code and accompanying documentation were developed with generative AI assistance for code development and technical writing under maintainer direction. The maintainer remains responsible for reviewing the implementation, validating results, and making release decisions. AI assistance does not constitute independent verification, provider endorsement, or release approval.
